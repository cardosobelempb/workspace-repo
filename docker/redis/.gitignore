# Envs — nunca suba arquivos com credenciais reais
.env
.env.prod
.env.test

# Mantenha apenas o exemplo versionado
# .env.dev pode ser versionado se não tiver segredos reais

node_modules/
dist/
build/
